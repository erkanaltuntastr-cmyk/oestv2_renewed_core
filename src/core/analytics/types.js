/**
 * @typedef {Record<string, number>} VolumeMap
 */

/**
 * @typedef {Record<string, number>} FrequencyMap
 */

/**
 * @typedef {Object} ProgressionPoint
 * @property {number} date
 * @property {number} volume
 */

/**
 * @typedef {Record<string, number>} PRRecord
 */

export {};
