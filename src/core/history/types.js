/**
 * @typedef {Object} HistoryEntry
 * @property {string} id
 * @property {number} date
 * @property {Array<{code: string, name: string, sets: number, reps: number, weight: number | null}>} movements
 * @property {number} totalVolume
 * @property {number} durationMinutes
 */

/**
 * @typedef {HistoryEntry} NormalizedHistoryEntry
 */

export {};
